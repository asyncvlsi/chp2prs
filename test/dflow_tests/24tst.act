import "24.act";
import "24_df_chp.act";

defproc test()
{
  chan(int) X;
  chan(int<1>) Y;
  
  df_testproc d(X,Y);

  int a;
  int<1> b;

  chp {
    X!15;
    Y?a;
    Y?b;
    [ a = 1 & b = 1 -> log ("SUCCESS") [] else -> log ("FAILURE: ", a, " b=", b) ]
  }
}
