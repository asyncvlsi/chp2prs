import "36.act";
import "36_df_chp.act";

defproc test()
{
  chan(int) X, A, B;
  chan(int) Y;
  
  df_testproc d(X,A,B,Y);

  int a, b, c;
  int i;
  bool success;

  chp {
      i := 0;
      success+;
       X!15; //Y?a; [ a != 5 -> success- [] else -> skip ];
       log ("init");
   *[  i < 10 ->
       X!18;
       log ("send 1");
       //Y?a; [ a != 5 -> success- [] else -> skip ];
       //log ("recv 1");
       B!13;
       log ("send 2");
       Y?c;
       log ("recv 2");
       [ c = 31 -> skip [] else -> success-; log ("FAILURE: c=", c) ];
    i := i + 1
    ];
    [ success -> log ("SUCCESS") [] else -> skip ]
  }
}
